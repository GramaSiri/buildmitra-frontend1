export const createMaterial = (req, res) => { res.send('createMaterial called!'); };

export const getMaterials = (req, res) => { res.send('getMaterials called!'); };

export const exitMaterialFlow = (req, res) => { res.send('exitMaterialFlow called!'); };
